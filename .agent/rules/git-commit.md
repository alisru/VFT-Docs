---
trigger: always_on
glob:
description: use git commit to commit the changes after every edit, do not use git push. Summarise changes, or if the changes are too massive, simply say "Massive changes"
---

