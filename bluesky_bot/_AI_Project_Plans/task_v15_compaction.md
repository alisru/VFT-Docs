# Active Task List: Calibration Drift Graph Axis Correction

- [x] Swap variables in `auditCalibration` inside `control_panel.html` to align morality ($u$) to horizontal (X) and will ($\psi$) to vertical (Y).
- [x] Correct the SVG text labels to reflect `u →` horizontally and `+ψ`/`−ψ` vertically.
- [x] Verify visual rendering of Calibration Drift graph.
