# Active Task List: Actor Leaderboard Dropdowns and Automatic Registry Actor Tagging

- [x] Import `extract_actors` in `rebuild_registries.py` and implement automatic backfilling/writing back of missing `actors` tags.
- [x] Add `toggleActorDropdown(actorId)` function and style updates to `control_panel.html`.
- [x] Modify `auditActorLeaderboard(stories)` in `control_panel.html` to support clicking on actor names and rendering dropdowns of related stories.
- [x] Run `rebuild_registries.py` to auto-tag the entire batch of stories and compile the updated registry.
- [x] Verify the dropdown functionality and selection mechanism in `control_panel.html`.
