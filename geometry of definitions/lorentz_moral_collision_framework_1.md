# Lorentz Moral Collision Framework
*Velocity on the moral tensor as trajectory prediction for intersection with fixed strain points on a relative infinite arbitrary frame*

DEPTH: standard

---

## Core Definitions

**Variable_Effective (Truth)** = the observable output of any system processing raw truth through its own motion

    Variable_Effective = Truth_0 / √[1 - (v_Control² / c_Evil²)]

**Truth_0** = the raw, unspun fact at rest. No velocity applied. Clear signal.

**v_Control** = velocity of a system toward the evil anchor — the speed at which it adds its own friction, spin, control layers, or extraction behaviour. Positive = moving toward (-2,-2). Negative = moving away.

**c_Evil** = the absolute limit. The speed at which a system becomes 100% control and 0% reality. The singularity of dogma. Maps to the Greater Evil attractor at (-1υ,-1ψ) on the moral tensor. The ±2 boundary is theoretically unreachable — c_Evil is the practical singularity at the attractor coordinate.

**γ (Lorentz factor)** = 1 / √[1 - (v²/c²)] — the scalar that measures how much local space and time must distort to keep c constant. In moral physics: how much truth must distort to accommodate the system's velocity toward the evil anchor.

**Operator conventions:**
- ^2 denotes a 2D vector on the (υ,ψ) plane, not scalar squaring
- / means "relative to" — vector proximity, not division
- v_Control² → v_Control(υ,ψ): exact moral and will coordinates of the acting system
- c_Evil² → c_Evil(-1υ,-1ψ): the Greater Evil fixed anchor point (±2 is unreachable extreme)

**The moral tensor axes:**
- υ (morality, horizontal): who this benefits — +υ LEFT = Other/Beings/Everyone, 0υ = neutral origin, -υ RIGHT = Only Me/My Group
- ψ (will, vertical): what the energy is doing — +ψ UP = Do/Will/Action, 0ψ = neutral, -ψ DOWN = Do Nothing/Passive/Malice

**The try^2 / catch structure:**

    try^2 { Circle | Recursion = 2 }   // stable meaning orbit
    catch { Ovals | n → ∞ }            // distortion / locked door

Circle = truth at rest, loop closed, harvest reachable. Ovals = truth under velocity, loop deformed, meaning dilated.

**The relative cube:** any point can be represented as (x,y,z,t) by drawing an arbitrary interaction frame around it and touching it to all extents. Coordinates are not properties of the point — they are descriptions of its relationship to the frame drawn around it.

---

## What This Framework Is

This is a unified collision and trajectory system for moral physics. It answers two questions simultaneously:

1. How distorted is the output of this system right now? (the dilation factor — current intersection depth)
2. How quickly will it reach total collapse? (the velocity vector — time-to-singularity)

Positive Lorentz velocity on the moral tensor indicates potential collision or alignment with a fixed position on the relative infinite arbitrary frame. When that fixed position is the evil anchor (-2,-2) on the strain tensor, the framework determines both how much the system's influence sphere currently intersects with that point temporally, and how quickly it will get there.

The dilation factor is not just a distortion measure — it is a collision depth reading. The velocity is not just a direction indicator — it is a trajectory prediction. Together they give you intersection geometry across time.

**The moral tensor coordinate space — six attractor points:**

    Greater Good:        (+1υ, +1ψ)   — The Good Truth     — Productive quadrant
    Lesser Evil:         (-1υ, +1ψ)   — The Bad Lie        — Reductive quadrant
    Lesser of two Evils: (+1υ,  0ψ)   — The Half Truth     — 0ψ preference axis, left
    Greater of two Evils:(-1υ,  0ψ)   — The Half Lie       — 0ψ preference axis, right
    Lesser Good:         (+1υ, -1ψ)   — The Good Lie       — Constructive quadrant
    Greater Evil:        (-1υ, -1ψ)   — The Bad Truth      — Regressive/Destructive quadrant

Unreachable extremes (boundary only, not attractors):
    (+2υ, +2ψ) — theoretical maximum good
    (-2υ, -2ψ) — theoretical maximum evil

Scale: 0.25–1.75 in practice. 0 unachievable absolute. ±2 unreachable boundary.

**Lorentzable variables:** any variable — Time, Money, Love, Truth — behaves relativistically under systemic motion. None are absolute. All contract or dilate based on v_Control:

    Variable_Effective = Variable_Rest / √[1 - (v²/c²)]

---

## The Six-Attractor Gravitational Field

The moral tensor is not a flat space with one forbidden zone — it is a gravitational field with six fixed attractor wells. Every actor position on the tensor is subject to the combined pull of all six simultaneously. The net trajectory at any moment is the vector sum of all six forces.

**Gravitational pull per attractor:**

    F_i = M_i / d_i²

Where M_i = the attractor's mass (its structural weight in the field) and d_i = current distance from the actor to that attractor. Closer attractors dominate. All six pull at once.

**Net velocity vector:**

    v_net(υ,ψ) = Σ F_i × direction_i    (i = 1 to 6)

The predicted scripted path is the integral of v_net over time — the actual trajectory the actor will follow given their current position and the full gravitational field.

**Relative position calculation:** for any actor at P(υ,ψ), calculate distance and bearing to all six attractors:

    d_GreaterGood        = |P - (+1υ,+1ψ)|
    d_LesserEvil         = |P - (-1υ,+1ψ)|
    d_LesserOfTwoEvils   = |P - (+1υ, 0ψ)|
    d_GreaterOfTwoEvils  = |P - (-1υ, 0ψ)|
    d_LesserGood         = |P - (+1υ,-1ψ)|
    d_GreaterEvil        = |P - (-1υ,-1ψ)|

The collision risk is not just proximity to Greater Evil — it is the ratio of pull from evil attractors vs good attractors. An actor equidistant from Greater Good and Greater Evil is in genuine tension. An actor slightly closer to Greater Evil is already being pulled faster toward it than away.

**The 0ψ preference axis — pure morality drift:** the two mid-axis attractors (Lesser of Two Evils and Greater of Two Evils) sit on the 0ψ line — zero will, pure morality. An actor on this axis has no active will component — they are drifting on υ alone. This is a distinct trajectory class: no arc-closure force, no harvest possible, no collision momentum — just passive lateral drift toward whichever υ attractor is closer. The 0ψ actor cannot complete the try^2 circle because ψ=0 means no recursive energy. They can only slide horizontally.

**Immediate vs long-term collision risk:** an actor may be closer to Lesser Evil than Greater Evil — their immediate collision risk is with the lesser attractor. But Lesser Evil at (-1υ,+1ψ) curves the long-term trajectory: once captured by Lesser Evil's well, the path bends further toward Greater Evil's pull. The scripted path must account for sequential attractor capture, not just nearest-first.

---

## Where It Operates — The Spatial Frame

The framework operates on a relative infinite arbitrary frame. There is no absolute coordinate origin. Every point exists only relative to other points.

**Constructing the interaction frame:** draw a relative cube around any particle and touch it to all extents. This creates the metric space that allows the point to exist as measurable data. The frame between two points defines the bounds of their interaction space.

**Doubling the bounds:** to capture full directionality for both particles in an interaction, double the bounds of the interaction frame. This encapsulates the influence sphere of each particle relative to the other.

**The fixed evil anchor:** Greater Evil sits at (-1υ,-1ψ) — a fixed attractor point on this infinite frame, not a moving target. Any actor with net velocity toward it is on an intersecting trajectory. The frame is infinite; the attractor is permanent; the only variables are the actor's velocity and current position.

**Worldtubes:** a 3D sphere moving through time generates a 4D hyper-cylinder — a worldtube. The swept volume of any actor on the moral tensor is their worldtube: their entire future probabilistic influence space encoded in one geometric shape. If that worldtube intersects the Greater Evil attractor (-1υ,-1ψ), collision is inevitable unless velocity changes.

**The oblate spheroid hit-box:** at relativistic speeds (v approaching c_Evil), the actor's influence sphere physically distorts:
- Radius perpendicular to motion: stays R
- Radius parallel to motion: contracts to R/γ

The hit-box flattens in the direction of travel. An actor moving hard toward (-1υ,-1ψ) is also compressing their own observable footprint in that direction — they become harder to read from outside their frame.

---

## Why Velocity Sign Matters

Positive v toward (-1υ,-1ψ) is not neutral motion. It is a collision trajectory. The swept volume of the actor is extending toward the Greater Evil attractor with every tick.

Negative v away from (-1υ,-1ψ) is active resistance — not just neutrality. At v = -1:

    Variable_Effective = Truth_0 / √[1 - (1/4)]
                       ≈ 1.15 × Truth_0

The 1.15 multiplier is a moral buffer. Moving away from the anchor slightly magnifies Truth_0 — the system gives the receiver more agency than the raw context alone provides. The circle expands slightly rather than compressing. This is the shield state.

The Lorentz factor squares the velocity (v²), so the magnitude of distortion is identical at +v and -v of the same speed. What differs is the data signature:
- +v (toward evil): output is compressed, approaching the anchor, blueshift equivalent — intense, concentrated, increasingly indistinguishable from the anchor's character
- -v (away from evil): output is stretched slightly, redshift equivalent — the actor is pulling away, the gap is widening

The alignment question: does this system's trajectory, extended temporally through its worldtube, intersect (-1υ,-1ψ)? If v is positive toward the Greater Evil attractor and sustained, yes. The only variables are speed (how fast) and current position (how far).

---

## How the Collision Mechanics Work

**Tracking position over time:**

    C_actor(t) = P_initial + V*t

The actor starts at position P on the (υ,ψ) tensor and moves at velocity V. Their position at any time t is calculable. Their position at the collision point (when their worldtube reaches the evil anchor) is:

    t_collision: solve |C_actor(t) - (-1υ,-1ψ)| ≤ R_influence

**The dilation factor as intersection depth:**

    depth = 1 - √[1 - (v_Control² / c_Evil²)]

At v=0: depth=0, no intersection, circle intact.
At v=0.5c: depth≈0.13, mild oval, partial intersection.
At v=0.99c: depth≈0.86, severe oval, near-total intersection.
At v=c: depth=1, singularity, full collision, truth undefined.

**The refresh rate / chunk lens:** the observer's processing window has fixed capacity per tick (----). A system moving at high v toward the anchor generates data at a rate that exceeds the observer's lens capacity. The lens concatenates — compresses the incoming signal along the axis of motion. This is why high-v systems look coherent and internally consistent from outside: their data is being chunk-compressed through the observer's lens, hiding the oval distortion.

**Sign rule for grid tracking:**
- Position and velocity same sign: moving away from origin
- Position and velocity opposite sign: moving toward origin
- Crossover: status flips the moment the actor passes through the reference point

**The net vector with a challenger:**

    v_Net = v_Control + (-v_Resistance)

If an actor pushes +1 toward (-1υ,-1ψ) and a challenger applies -1, v_Net = 0. The actor's worldtube stops extending toward the attractor. The oval resets toward circle. The locked door stays open.

**The full try^2 execution:**

    try^2 {
        if (Recursion == 2) {
            Harvest = (Action → Effect);    // circle holds, meaning stable
        }
        else if (Recursion > 2) {
            throw "Overthinking_Rot";       // spiral, no harvest
        }
    }
    catch (Ovals) {
        if (Oil == 0) {
            return "Locked_Door";           // singularity reached, truth undefined
        }
        else {
            Reset(Context);                 // challenger present, re-centre
        }
    }

Oil = the energetic capacity to sustain the negative counter-vector. When oil runs out, the challenger can no longer hold v_Net at zero. The actor's positive velocity resumes. The worldtube continues extending toward the anchor.

**The meaning integral:**

    Meaning = ∫ [0 to 2] (Actions → Effect / Context) d(Lorentz)

Lower limit 0 = contextual base — the given reality, the seed. Without it, the action has no place to land.
Upper limit 2 = harvest threshold — the recursion trigger. Effect loops back to become the next action.
d(Lorentz) = the distortion gradient — integrating across every micro-step of friction the process applies.

---

## Origin — Where These Mechanics Come From

**Why c is invariant:** c is the refresh rate of the universe — the maximum speed at which causality propagates between nodes. No observer can measure a distorted c. What distorts to protect c is the local frame: time slows, distances shrink. In moral physics, c_Evil plays the same role — the absolute limit that the framework cannot exceed without collapsing into undefined output.

**Why the circle:** the circle is the geometry of stable recursion. A closed loop where effect feeds back into action at exactly n=2. Any velocity applied to this loop deforms it into an oval — two foci pulling apart, the gap between intent and outcome. The oval is not a failure state to be avoided but a diagnostic: its eccentricity is a direct reading of v_Control.

**The relativity of simultaneity as data desync:** observers at different velocities disagree on what "now" means. An actor moving at high v toward (-1υ,-1ψ) and an observer at rest will timestamp the same events differently. The actor's internal experience is normal (proper time is always normal). From outside, the actor appears to be operating in slow motion — fewer events per tick of the observer's clock — but those events are highly compressed spatially. This is why high-v extraction systems appear productive and coherent while accumulating collision depth.

**The invariant spacetime interval:**

    Δs² = c²Δt² - (Δx² + Δy² + Δz²)

All observers disagree on Δt and Δx individually but agree on Δs². In moral physics: observers may disagree on the exact coordinates of an actor's position, but the physical collision — the worldtube intersection with the evil anchor — is an invariant truth no frame can dilate away.

---

## Fractal Minkowski Raytracing

The framework is raytracing with relative velocities over fractal Minkowski space to avoid a known attractor.

**The space is fractal Minkowski.** Standard Minkowski gives one flat 4D spacetime. The Qqci tensor gives nested 4D spacetimes — each interaction frame (each relative cube drawn between two points) is its own Minkowski space, and the 7-plane structure repeats self-similarly at Q, q, and c depth. Raytracing this space means every intersection test at Q level can spawn the same test at q level if you drill, and again at c level inside that.

**The goal is path planning, not hit detection.** Standard raytracing finds what the ray hits. Here the Greater Evil attractor is a black body — rays that reach it don't bounce, they terminate (v=c, Truth=undefined). The algorithm is: find the path that closes the arc within r=2 without clipping (-1υ,-1ψ). Pathfinding in relativistic moral spacetime with a forbidden zone.

**The arc is the constraint that makes it non-standard.** A straight ray never returns. The harvest requires the ray to curve back to its origin within the recursion bound — that's the circle. Velocity deforms the arc into an oval. Exceeding the bound straightens the arc into a line. The intersection test against (-1υ,-1ψ) runs simultaneously with the arc-closure test on the same ray.

**The forbidden zone exists at every fractal depth.** There is a (-1υ,-1ψ) equivalent at Q level, at q level inside each Q, at c level inside each q. An actor can be globally moving away from the macro attractor while locally clipping a micro version of it at q or c depth. Chunk lens compression means micro-level clips are invisible from outside until oval distortion has already accumulated. This is why the convergence test drills — you're testing the sub-rays at every fractal depth for local attractor contacts the macro view misses.

**The 2×2 outcome space:**

    Arc closes + misses (-1υ,-1ψ):          ideal — harvest achieved, no collision
    Arc closes + clips (-1υ,-1ψ):           partial collision — oval distortion, harvest degraded
    Arc fails to close + hits (-1υ,-1ψ):    full collision — singularity, locked door
    Arc fails to close + misses (-1υ,-1ψ):  overthinking rot — no harvest, no extraction

---

## Effects and Consequences

**Relative zero — shared sphere of influence:** when two actors reach v=0 relative to each other, they share a frame. No time dilation, no length contraction, no desync. Their chunk lenses synchronise. Their observations are mathematically identical. This is alignment — not agreement of opinion but agreement of reference frame.

**The truth equation:**

    ___ == knowledge.context[] == observations

For this to hold, knowledge.context[] must act as a clear window — v_Net = 0 relative to the event, no layers added. When it holds, raw truth passes through unchanged. The circle completes.

**Collision invariance:** the intersection of two worldtubes is an absolute fact across all frames. Observers may disagree on when and where the collision occurred in their local coordinates — they cannot disagree that it occurred. A system that has fully reached (-1υ,-1ψ) has collided. No reference frame can un-collide it.

**The four dilation states:**

State A — v=0: Variable_Effective = Truth_0. Circle intact. No collision trajectory. Output equals input.

State B — v approaching c_Evil: oval deepening, output magnified (7× at v=0.99c). Looks like wisdom. Is recursive rot. Worldtube extending toward Greater Evil attractor.

State C — v=c_Evil: singularity. Truth_0/0 = undefined. Infinite noise. Locked door. Worldtube has reached (-1υ,-1ψ). Full collision.

State D — v=c and context=0: 0/0 = indeterminate. The integral collapses to ∫[0 to 0] = 0. No meaning generated regardless of processing power. The lamp does not exist.

**The ideal:** v_Control → 0, solid contextual floor acknowledged, recursion held at n=2.

    Variable_Effective = Truth_0 / 1 = Truth_0
    Meaning = ∫ [0 to 2] (Actions → Effect / Context) d(1)

Circle. Oil present. Door open. Worldtube oriented toward Greater Good (+1υ,+1ψ), diverging from Greater Evil attractor. Negative velocity relative to evil. Moral buffer active.
