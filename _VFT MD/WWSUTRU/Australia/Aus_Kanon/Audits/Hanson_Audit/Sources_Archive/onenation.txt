SOURCE: [^onenation]
CITATION: Pauline Hanson's One Nation, Policies
URL: https://www.onenation.org.au/policies (redirects to /issues)
FETCHED: 2026-07-04, via Claude-in-Chrome browser-use (script/requests fetch and plain web_fetch both returned only a thin ~510-char shell; this is a genuine JS-rendered React page)
STATUS: fetched via rendered browser, confirmed the page genuinely is a thin tile-grid hub (not a truncation artifact) -- real policy content lives on each individual policy sub-page, not this hub page itself

---EXTRACTED TEXT (rendered)---

Title: One Nation Policies on Issues Affecting Australia
URL renders to: https://www.onenation.org.au/issues

Visible content: "LOWERING PRICES FOR HOUSEHOLDS, BUSINESSES AND INDUSTRIES / READ MORE" (a single policy tile headline, likely the first of several tiles that load individually via JS as the user scrolls; the accessibility/text-extraction pass only captured this one visible tile).

VERIFICATION LOG: Confirms this hub page is not a rich source of directly quotable text in its own right -- it is a navigation/index page linking to individual policy sub-pages (e.g. /tax, /firearms, /net-zero-destroying-australia, etc.), each of which has real substantive text (see those tags' own archive files). If a node cites [^onenation] generically for "party platform," that's fine as a hub reference, but any specific claim should be re-pointed to the actual policy sub-page that contains it.
