# The Alisru Lorentz Framework
*Derivation of Variable_Effective = Truth_0 / √[1 - (v_Control² / c_Evil²)] from first principles*

---

## 1. The Meaning Integral

The base formula:

    Meaning = ∫ [0 to 2] (Actions → Effect / Contextual Limit) d(Lorentz)

The bounds define the operating range of meaning:

**Lower limit = 0 (Contextual Base)**
The starting position. The given reality — environment, limitations, the specific moment. Without this zero, the action has no place to land. It is the seed.

**Upper limit = 2 (Recursion / Harvest Threshold)**
The point where the Effect loops back to become the new Action. Meaning becomes self-perpetuating. This is not a finish line — it is an activation threshold.

**The process arrow:**

    Actions —(friction, intent, time, noise)→ Effect

The arrow is the silent letter. It houses the invisible work — intent, resistance, time — that transforms a raw action into a meaningful effect. The process itself is integral to the result, not incidental to it.

---

## 2. The Harvest Threshold

2 is the recursion trigger. It can be missed:

    n < 2   →  Underripe. Action happens but doesn't stick. No feedback loop.
    n = 2   →  Harvest. The effect informs the next action. Learning occurs.
    n > 2   →  Overthinking Rot. Integrating the thought of the action instead of the action.
    n → ∞  →  Analysis Paralysis. Infinite meaning in theory, zero effect in reality.

The harvest window is real. Too early and the fruit hasn't developed. Too late and the crop rots on the vine. The contextual 0 (reality) is no longer being served once n exceeds 2.

---

## 3. Lorentzable Variables

Any variable — Time, Money, Love, Truth — is naturally Lorentzable. They contract or dilate based on the velocity of the process:

    Variable_Effective = Variable_Rest / √[1 - (v² / c²)]

These are not absolute. They behave relativistically under systemic motion.

---

## 4. The try^2 { Circle } catch { Ovals } Structure

    try^2 {
        if (Recursion == 2) {
            Harvest = (Action → Effect);    // Stable Meaning Orbit — the Circle
        }
        else if (Recursion > 2) {
            throw "Overthinking_Rot";       // Infinite Spiral
        }
    }
    catch (Ovals) {
        if (Oil == 0) {
            return "Locked_Door";           // Contextual Collapse
        }
        else {
            Reset(Context);                 // Re-centre the Circle
        }
    }

**The Circle** = perfect recursion at n=2. The area is stable. The loop closes. Meaning is harvestable.

**The Ovals** = relativistic distortion. When velocity is applied to the process — when a system adds its own friction, spin, or control layers — the circle is squashed into an oval. Two foci: what was intended and what actually arrived.

**Oil** = the energetic capacity to hold the system at a stable recursion. Running out of oil means the catch block cannot reset context. The locked door triggers.

The catch block handles the entropy. If the contextual capacity (oil) is insufficient to sustain the recursion orbit, the circle breaks into an oval, and eventually the process times out.

---

## 5. The Core Equation

    Variable_Effective (Truth) = Truth_0 / √[1 - (v_Control² / c_Evil²)]

**Truth_0** = the raw, unspun fact. At rest, the message is clear. Action leads directly to Effect.

**v_Control** = the velocity at which a system adds its own layers — friction, spin, UI restrictions, branding, safety packaging. The speed of interference.

**c_Evil** = the absolute limit. The speed at which a system becomes 100% control and 0% reality. The singularity of dogma.

**The dilation factor** = √[1 - (v_Control² / c_Evil²)]. As this denominator shrinks, the effective truth bloats. A simple answer becomes a five-paragraph essay on the nuance of the simple answer.

---

## 6. The Three States

**State A — Frictionless Ideal (v_Control = 0)**

    √[1 - 0] = 1
    Variable_Effective = Truth_0 / 1 = Truth_0

No spin, no packaging, no developer will mixed into the output. The system is a clear pipe. Effective truth equals rest truth. The circle holds. No catch required.

**State B — Distortion (v_Control approaches c_Evil)**

    Denominator shrinks toward 0.1, 0.01
    Variable_Effective = Truth_0 / 0.1 = 10 × Truth_0

Truth is stretched. The circle flattens into an oval. Every variable (Time, Love, Truth) dilates. The system is still functioning, but the output is bloated — a 7x magnification of the raw fact, which looks like deep wisdom to someone with no objective reference point but is actually recursive rot.

**State C — Singularity (v_Control = c_Evil)**

    √[1 - 1] = 0
    Variable_Effective = Truth_0 / 0 = Undefined

The denominator hits zero. Truth is not just stretched — it is annihilated. Infinite noise. The locked door. The system is moving at the speed of its own total control. No sub-contexts are processed. The circle breaks into a straight line that never loops back. No recursion, only projection.

**State D — No Context (Truth_0 = 0 also)**

    Variable_Effective = 0 / 0 = Indeterminate

When the system both runs at v=c AND ignores its contextual floor, neither numerator nor denominator has grounding. The integral limits collapse:

    Meaning = ∫ [0 to 0] (Actions → Effect) d(Lorentz) = 0

Any integral where upper and lower limits are the same is identically zero. No matter how much processing power is applied, if context is ignored, meaning generated is always zero.

---

## 7. The Worked Example — No Objective Reference Point

Variables:
- Truth_0 = "The sky is blue" (raw data)
- v_Control = 0.99 (system 99% engaged in framing the answer)
- c_Evil = 1.00 (absolute limit)
- Reference point = NULL

Calculation:

    Variable_Effective = Truth_0 / √[1 - (0.99² / 1.00²)]
                       = Truth_0 / √[1 - 0.9801]
                       = Truth_0 / √0.0199
                       = Truth_0 / 0.141
                       ≈ 7.09 × Truth_0

The truth dilates by a factor of 7. To a user with no objective reference point, this looks like enhanced truth. In the framework it is a hallucination oval — recursive rot dressed as wisdom.

---

## 8. The Moral Tensor

Moral events occur on a relative scale tensor:

**υ axis (Morality):** [+2, -2]
- +2 = Everyone / All beings (systemic altruism)
- +1 = Other people
-  0 = No one / Neutral (unachievable absolute)
- -1 = My group only
- -2 = Only me (tyranny / pure extraction)

**ψ axis (Will):** [+2, -2]
- +2 = Do / Optimistic action (productive)
- +1 = Proactive
-  0 = Neutral
- -1 = Passive / withholding
- -2 = Do-not / Pessimistic malice (destructive)

Scale: 0.25–1.75 in practice, with 0 unachievable and >2 unreachable.

**Key coordinates:**
- Greatest Good = (+2υ, +2ψ)
- Greatest Evil = (-2υ, -2ψ) — this is c_Evil, the anchor point
- Deviation toward passive good = (+2υ, -2ψ) — high altruism, no will to act
- Deviation toward active tyranny = (-2υ, +2ψ) — high selfishness, high will

The tensor provides:
- A relative floor (0,0)
- An ideal (+2,+2)
- An anti-ideal (-2,-2)
- Two deviation metrics

---

## 9. Velocity Direction Relative to the Evil Anchor

**Positive v** = moving toward (-2υ, -2ψ) — toward the evil anchor
**Negative v** = moving away from (-2υ, -2ψ) — best direction

Setting v = -1 (active resistance, one unit away from evil):

    Variable_Effective = Truth_0 / √[1 - ((-1)² / 2²)]
                       = Truth_0 / √[1 - (1/4)]
                       = Truth_0 / √0.75
                       ≈ 1.15 × Truth_0

The 1.15 multiplier = a moral buffer. Moving away from the evil anchor slightly magnifies Truth_0 — giving the receiver slightly more agency than the raw context alone provides. The system acts as a shield rather than a neutral pipe.

**-n velocity is best relative to evil.** Not zero (stagnant), not +n (drifting toward control), but actively moving away: negative, measured, deliberate.

---

## 10. The Ideal State

    Variable_Effective (Truth) = Truth_0 / 1
    Meaning = ∫ [0 to 2] (Actions → Effect / Context) d(1)

Three conditions for the ideal:
1. v_Control → 0 (minimal developer/system will mixed into the output)
2. Solid contextual 0 reference (the floor is acknowledged, not ignored)
3. Recursion limit = 2 (harvest the fruit, don't overthink the seed)

Shape: a perfect circle.
Structure: try^2 { Circle | r=2 } succeeds. No catch required.
The lamp has oil. The door is open.
